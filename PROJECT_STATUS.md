# Project Status

- Current stage: Serving qualification CLOSED with read-only analysis complete; distributed launch validation and canonical warehouse/release integration next
- Current Git identity: model freeze source `553796b`; training freeze source `0008038`; serving freeze source `04f740b`; serving qualification Git SHA `9a02578`; local identity `Jinwoo Bae <bjw-0907@hanmail.net>`
- Active environment: Windows PowerShell control plane; WSL `.venv` TRAIN/MODEL/ANALYSIS on Python 3.12.14 and PyTorch 2.14.0+cu130; isolated WSL `.venv-serve` on Python 3.12.14, PyTorch 2.13.0+cu132, and vLLM 0.28.0
- Completed stages: foundation/data; deterministic M0/M1 validation; compact LoRA selection; protected model qualification and release decision; frozen six-run SF-TRAIN-v1 qualification; training figures F04-F06/F13; T4 dynamic-padding plus compile selected; frozen 36-point SF-SERVE-v2 qualification; serving figures F07-F10 and serving F13 diagnostic; serving release review
- Protected-data access status: CLOSED for scientific model work. V1 and V2 failures remain preserved. V3 re-scored complete immutable V2 outputs under a corrected frozen parser and passed cardinality/pairing gates. No further model/prompt/parser tuning is permitted under these identities.
- Important artifacts: `artifacts/analysis/model/model_qualification_sf_model_v3.json`; `artifacts/analysis/training/training_qualification_sf_train_v1.json`; `artifacts/analysis/serving/serving_qualification_sf_serve_v2.json`; `artifacts/warehouse/serving_requests.parquet`; `artifacts/warehouse/serving_runs.parquet`; `configs/serving/slo.yaml`; figures F01-F10 and thermal/power diagnostics
- Active failures: historical model/parser and training-profiler failures remain preserved. Serving preserves the WSL vLLM V2-runner UVA failure, a qualification preflight config-ID rejection with no requests sent, and a repeatable post-measurement vLLM EngineCore force-kill/leaked-semaphore shutdown defect. All 2,304 qualification requests succeeded.
- Training qualification status: CLOSED for measurement and ANALYSIS complete. Three paired runs processed 35,382 identical non-padding tokens each; T4's steady throughput gain and projected target wall-time gate passed, but n=3 and material order/thermal associations require explicit qualification.
- Serving development status: SF-SERVE-v1 was closed as an invalid quality workload because the 128-token cap truncated 98.2% of development-validation M0 outputs. SF-SERVE-v2 uses the development-selected 512-token cap (98.6% completion coverage), the same immutable POLICY corpus, and a frozen ScaleForge-only SLO: TTFT p95 <= 500 ms, TPOT p95 <= 140 ms, E2E p95 <= 33 s, and error rate <= 1%, with paired quality and parse-failure non-regression. The HF anchor is concurrency 2; all six baseline development points completed with zero service failures.
- Serving qualification status: CLOSED. Thirty-six frozen points (two runtimes x six concurrencies x three fresh-server replicates) and 2,304 measured requests passed artifact/cardinality checks with zero request failures. At concurrency 2, the max SLO-compliant point for both runtimes, S2 reached 0.2070 versus S0's 0.1208 requests/s: +71.39% median paired improvement (95% three-replicate bootstrap interval 51.01% to 89.52%). S2 reduced p95 TPOT 65.19% and p95 E2E 34.75%, but increased p95 TTFT from 86.86 to 443.24 ms; its hierarchical TTFT interval extends above the 500 ms point-estimate gate.
- Serving decision: REVIEW. All frozen request-path gates pass and S2 vLLM V1-runner at concurrency 2 is the selected experimental runtime. Unqualified production readiness is withheld because every vLLM shutdown force-killed an idle EngineCore and leaked a semaphore; repeated-corpus prefix-cache carryover, n=3 uncertainty, and clock/power associations are explicit limitations.
- Long-run budget result: the six predeclared restart-isolated blocks completed without changing the 5.2-hour budget or rerunning unfavorable points. Large high-concurrency variation was retained and analyzed rather than optimized away.
- Next action: validate the distributed launch path on the single-GPU topology, mark numerical multi-GPU qualification BLOCKED_EXTERNAL, then integrate the canonical DuckDB warehouse and independent release gates
